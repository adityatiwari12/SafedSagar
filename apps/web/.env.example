VITE_API_BASE_URL=http://localhost:8001

# /chat and /escalations are now implemented (apps/api/app/chat/router.py).
# Set to "true" only to develop the UI without a running backend.
VITE_USE_MOCK_CHAT=false
